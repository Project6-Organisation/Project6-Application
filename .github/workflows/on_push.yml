name: Integration Workflow

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

permissions:
  contents: write
  packages: write
  issues: write
  pull-requests: read

jobs:
  call-generic-pipeline-main:
    uses: Project6-Organisation/Project6-Integration/.github/workflows/Integration.yml@main
    secrets: inherit