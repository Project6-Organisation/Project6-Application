name: Trigger Integration Workflow
 
on:
  workflow_dispatch:
    inputs:
      info:
        description: "Use this to trigger the workflow manually"
        required: false

permissions:
  contents: write
  packages: write
  issues: write
  pull-requests: read
 
jobs: 

  call-generic-pipeline-develop:
    uses: Project6-Organisation/Project6-Integration/.github/workflows/_01_Integration.yml@feat/test-dispatch
    with:
      origin: java
    secrets: inherit