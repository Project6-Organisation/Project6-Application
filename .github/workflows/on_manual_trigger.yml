name: Trigger Integration Workflow
 
on:
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  issues: write
  pull-requests: read
 
jobs: 

  call-generic-pipeline:
    uses: Project6-Organisation/Project6-Integration/.github/workflows/_01_Integration.yml@feat/test-dispatch   
    secrets: inherit