name: Integration Workflow

on:
  push:
    branches:
      - "develop"
      - "main"
    
permissions:
  contents: write
  packages: write
  issues: write
  pull-requests: write
 
jobs:  
  Artefact:
    # Triggered on :
    # - on merge to develop or main branch (the event is push)    
    needs:
      - Generic-pipeline-main
    uses: Project6-Organisation/Project6-Integration/.github/workflows/_02_Artefact.yml@main
    secrets: inherit